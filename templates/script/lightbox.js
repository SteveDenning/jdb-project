$(document).ready(function() {
	$(".fancybox").fancybox();
});
// $.getJSON('/script/json/fancybox.json', function(data) {		
// 	$(data.imageList).each(function(index) {
// 		addImage(data, index);
// 	});
// });
// function addImage(data, index) {
// 	$('.fancyboxWrapper').append('<a class="fancybox" rel="group" href="/images/content/carousel/' + data.imageList[index].largeImage + '"><img src="/images/content/carousel/'+ data.imageList[index].smallImage + '" alt="' + data.imageList[index].altText + '" /></a>');
// };
